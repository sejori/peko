import { assert } from "https://deno.land/std@0.174.0/testing/asserts.ts"
import {
  generateSitemap
} from "../../utils/sitemap.ts"

Deno.test("UTIL: sitemap", async (t) => {  
  await t.step("generate sitemap", () => {
    // use mock server?

    // assert(base.has("Content-Type") && base.has("Authorization"))
  }) 
})